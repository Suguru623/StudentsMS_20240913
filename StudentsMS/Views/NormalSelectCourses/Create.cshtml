@model StudentsMS.Models.SelectCourse

@{
    ViewData["Title"] = "新增課程";
    Layout = "_LayoutNS";
}

<h1 class="text-center shadow my-3 p-4">@ViewData["Title"]</h1>

@* <h4>SelectCourse</h4> *@
<div>
    <a asp-action="Index"><i class="bi bi-arrow-left-circle fs-4"></i></a>
</div>
<hr />
<div class="row">
    
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="StuID" class="control-label"></label>
                <input name="StuID" id="StuID" class="form-control" Value="@ViewBag.StuID" readonly></input>
            </div>
           @*  <div class="form-group">
                <label asp-for="CourseID" class="control-label"></label>
                <select asp-for="CourseID" class ="form-control" asp-items="ViewBag.CourseID"></select>
            </div>
            <div class="form-group">
                <label asp-for="DeptID" class="control-label"></label>
                <select asp-for="DeptID" class ="form-control" asp-items="ViewBag.DeptID"></select>
            </div> *@
           @*  <div class="form-group">
                <label asp-for="DeptID" class="control-label"></label>
                <select id="DeptID" name="DeptID" class="form-control">
                    @foreach (var item in ViewBag.selectCourse)
                    {
                        <option value="@item.DeptID">@item.Dept.DName</option>
                    }
                </select>
            </div> *@
            <div class="form-group">
                <label asp-for="CourseID" class="control-label"></label>
                <select id="CourseID" name="CourseID" class="form-control">
                    @foreach (var item in ViewBag.selectCourse)
                    {
                        <option value="@item.CourseID">@item.Course.CName</option>
                    }
                </select>
            </div>
           
           @*  <div class="form-group">
                <label asp-for="CTID" class="control-label"></label>
                <select asp-for="CTID" class ="form-control" asp-items="ViewBag.CTID"></select>
            </div> *@
            <div class="form-group">
                <input type="submit" value="確定新增" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@* <div>
    <a asp-action="Index">返回選課清單</a>
</div> *@

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
